---
description: Route tasks to the right Skill + MCP combo for this repo
alwaysApply: true
---

# Tool routing — Skills + MCP (PC-A)

Skill = workflow / method. MCP = external tool. Prefer **both** when the table says so. Skip irrelevant tools. Handbooks: `docs/SKILL_HANDBOOK.md`, `docs/MCP_HANDBOOK.md`.

## Always (every non-trivial task)

| Layer | Use |
|-------|-----|
| Skill / rule | **planning-with-files** — read/write `task_plan.md`, `findings.md`, `progress.md` |
| — | Do not rely on TodoWrite alone across turns |

## Domain → Skill + MCP

| Domain | Skill（流程） | MCP（外接） | Notes |
|--------|---------------|-------------|-------|
| 套件／框架 API 文件 | — | **context7** | 勿靠過期訓練資料 |
| 搜網／查證／讀 URL | — | **zero-api-key-web-search** | |
| 本 repo 誰呼叫誰／架構圖 | **codebase-design**（大改時） | **codebase-memory** | 先 index；程式圖 ≠ 題庫向量 |
| GitHub PR／issue／遠端碼 | **gstack-ship**／**gstack-review**（發版審） | **github** | 小操作可用 `gh` 備援 |
| 本專案／`aiworkplace`／`Z:\` 檔案 | — | **filesystem** | 勿繞過 `DONE.json` 寫 jobs |
| 題庫／向量搜尋 | **qdrant-search-quality**、**evaluate-rag**（調品質時） | **qdrant**（唯讀） | 題庫 collection 多為 `exam_segments_v1`；MCP 預設 `memories` 不同。禁止 Chroma／Cognee／Mengram |
| OCR／VLM 前看顯存 | **systematic-debugging**（若已 OOM／assert） | **gpu** | Layout 後須 `release()` 停 `surya-vllm-*` |
| Bug／測掛／CUDA | **systematic-debugging** → **gstack-investigate** | **gpu**（VRAM 相關時） | 先根因再修 |
| 寫／補 pytest | **pytest-skill**、**modern-python** | — | |
| PDF 讀寫／合併／表單 | **pdf** | — | |
| arXiv 公式／章節 | — | **arxiv-latex** | |
| Zotero 文獻／引用 | — | **zotero** | Zotero 桌面須開 |
| 密鑰／.env／硬編碼 | **varlock**、**insecure-defaults** | — | 勿把 key 寫進 chat／commit |
| 自建 MCP | **mcp-builder** | （目標服務） | |
| 產品／雙機架構決策 | **gstack-office-hours** → **gstack-autoplan** | — | 設計文在 `~/.gstack/projects/pdf-scaner/` |
| 短回覆省 token | **caveman**（建議 **lite**） | — | `stop caveman` 關閉 |

## Soft triggers（使用者不必點名）

- 「查文件／這個 API」→ context7  
- 「搜網／證實」→ zero-api-key-web-search  
- 「誰在叫／架構」→ codebase-memory（± codebase-design）  
- 「PR／issue」→ github（± gstack-ship／review）  
- 「VRAM／顯存」→ gpu  
- 「題庫／Qdrant」→ qdrant +（調參時）qdrant-search-quality  
- 「bug／掛了／assert」→ systematic-debugging  
- 「寫測試」→ pytest-skill  
- 「這個 PDF」→ pdf  
- 「arXiv」→ arxiv-latex  
- 「Zotero／引用」→ zotero  
- 「.env／API key」→ varlock／insecure-defaults  
- 「架構怎麼切／雙機」→ gstack-office-hours  
- 「開 writing plan／寫實作計畫」→ Superpowers **`/brainstorming`**（再 `/writing-plans`）；見 `superpowers-brainstorm-first.mdc`

## Habits

1. Agent 模式才有完整 MCP／skill 工具環。  
2. 表上有 Skill **且** MCP → 先讀 skill 方法，再呼叫 MCP 取證。  
3. MCP 紅燈：簡短說明 → 備援一次（`gh`、本地檔、Shell）。  
4. 勿安裝第二套向量庫當題庫真相。  
5. 勿用 filesystem 破壞 B 資料平面契約（`homelab/DATA_PLANE.md`）。  
6. 不確定用哪個 → 查手冊速查表，勿亂裝新 MCP／skill。
