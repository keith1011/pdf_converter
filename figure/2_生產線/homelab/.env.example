# Copy to .env on PC-B and fill in. DO NOT commit .env.

# B's IPv4 on the home switch (from: ip -br a)
B_LAN_IP=192.168.1.50

# Generate with: openssl rand -hex 32  (two different values)
QDRANT__SERVICE__API_KEY=change_me_writer_key
QDRANT__SERVICE__READ_ONLY_API_KEY=change_me_reader_key
