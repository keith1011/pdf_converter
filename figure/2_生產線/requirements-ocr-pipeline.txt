# DEPRECATED: use uv (source of truth is pyproject.toml + uv.lock).
#
# Core OCR feedstock:
#   uv sync
#   uv run python run_ocr_pipeline.py ...
#
# Optional Surya (still separate — Pillow pin issues on some Pythons):
#   uv pip install surya-ocr --no-deps
