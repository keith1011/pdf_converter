# Dependencies for the NEW OCR pipeline (run_ocr_pipeline.py)
# Install into your .venv (Python 3.14 note: avoid rebuilding old Pillow):
#
#   .\.venv\Scripts\Activate.ps1
#   pip install -U pillow
#   pip install -r requirements-ocr-pipeline.txt
#   pip install surya-ocr --no-deps
#   pip install pydantic opencv-python-headless pypdfium2 python-dotenv filetype platformdirs

pymupdf>=1.24.0
Pillow>=10.0.0
PyYAML>=6.0
torchvision>=0.19.0
transformers>=4.49.0
accelerate>=0.33.0
bitsandbytes>=0.43.0
pydantic>=2.0
pydantic-settings>=2.0
opencv-python-headless>=4.8
pypdfium2>=4.30
python-dotenv>=1.0
filetype>=1.2
platformdirs>=4.0

# Dev / unit tests
pytest>=8.0

# Install separately to avoid pip pulling an ancient Pillow source build on Py3.14:
#   pip install surya-ocr --no-deps
